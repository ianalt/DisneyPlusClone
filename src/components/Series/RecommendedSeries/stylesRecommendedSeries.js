/* eslint-disable prettier/prettier */
import { StyleSheet } from 'react-native';

const stylesRecommendedSeries = StyleSheet.create({
    container: {
        marginVertical: 15,
        /* borderColor: '#f00',
        borderWidth: 1, */
    },

    title: {
        fontSize: 18,
        color: '#fff',
        marginLeft: 10,
        marginBottom: 5,
    },

    img: {
        borderRadius: 15,
        width: 90,
        height: 125,
    },

    btnRecommendedSeries: {
        borderRadius: 15,
        marginHorizontal: 10,
    },
});

export default stylesRecommendedSeries;
